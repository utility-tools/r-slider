(function(){
    const slider = new RSlider({
        selector: document.querySelector('#r-slider'),
        gap: 20, // in px
        visibleItems: 3, // in px
        edgeItemWidth: 100, // in px
        animationDuration: 500 // in milliseconds
    });
})();